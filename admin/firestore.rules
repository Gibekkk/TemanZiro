rules_version = '2';

// ⚠️ TEMPORARY DEBUG RULES — allow all reads/writes.
// Revert to a locked-down ruleset before production use.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

import { StyleSheet } from "react-native";

export default StyleSheet.create({
  moneyButton: {
    flexDirection: "row",
    alignItems: "center",
    gap: 6,
    paddingVertical: 8,
    paddingHorizontal: 14,
    borderRadius: 20,
  },
  moneyText: {
    fontSize: 14,
    fontWeight: "600",
  },
});
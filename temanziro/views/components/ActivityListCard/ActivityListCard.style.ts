import { StyleSheet } from "react-native";

export default StyleSheet.create({
  activityCard: {
    borderWidth: 1,
    borderRadius: 12,
    padding: 16,
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
    gap: 12,
    width: "48%", 
  },
  label: {
    fontWeight: "500",
    fontSize: 15,
  },
});